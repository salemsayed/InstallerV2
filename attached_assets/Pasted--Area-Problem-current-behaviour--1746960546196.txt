| # | Area                              | Problem (current behaviour)                                                                                                                                      | Desired fix (acceptance criteria)                                                                                                                                                                                                                                                                                                              |
| - | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **Seed script**                   | A dummy user with mobile **`01234567890`** is inserted on every run.                                                                                             | *Remove* that hard-coded insert. The seed script must only load data passed through environment variables (`SEED_MOBILE`, `SEED_PASSWORD`, etc.) or skip user creation entirely. Add a unit test that fails if an extra user is present.                                                                                                       |
| 2 | **Installer Dashboard ⇒ Header**  | Company logo is missing / broken while it renders fine elsewhere.                                                                                                | Logo SVG/PNG should render at `48 px` height with aspect ratio preserved, dark-mode compatible, and linked to “/installer/home”. Compare to rendering in public pages to ensure parity.                                                                                                                                                        |
| 3 | **Installer Dashboard ⇒ Badges**  | Status badges (e.g. “Pending”, “Completed”) are rendered twice for each item.                                                                                    | Only one badge per status. Add a regression test that mounts `<JobCard>` and checks for a single `.status-badge` element.                                                                                                                                                                                                                      |
| 4 | **Auth error UX**                 | When an unauthorised user hits a protected route they receive the raw 401 JSON (`{"statusCode":401,"message":"Unauthorized"}`) instead of the styled error page. | Intercept 401 in Axios interceptor → redirect to `/error/unauthorised`. Create a friendly page matching design system: icon, “You’re not invited yet”, CTA button “Request access”.                                                                                                                                                            |
| 5 | **Admin Dashboard ⇒ Users table** | Edit and Delete actions have no effect. `PUT /api/admin/users/:id` returns 200 in dev console but the UI doesn’t update; Delete button does nothing.             | a) **Edit:** After saving, optimistic-update the React Query cache so the row shows new data without full reload. b) **Delete:** Show confirmation modal, call `DELETE /api/admin/users/:id`, remove row from table, toast “User deleted”. Add e2e test using Playwright to edit a user’s name and delete another user, asserting DOM updates. |
